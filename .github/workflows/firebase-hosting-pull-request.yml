name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  contents: read
  id-token: write # Required for Workload Identity Federation
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Dependencies
        run: npm ci
      - name: Authenticate with Google Cloud
        id: auth
        uses: google-github-actions/auth@v2
        with:
          project_id: 'bridge2partners-staging'
          workload_identity_provider: '${{ secrets.WIF_PROVIDER }}'
          service_account: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMAIL }}'
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
      - name: Deploy to Firebase PR Channel
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
          GOOGLE_APPLICATION_CREDENTIALS: '${{ steps.auth.outputs.credentials_file_path }}'
        run: firebase hosting:channel:deploy pr-${{ github.event.pull_request.number }} --project bridge2partners-staging --non-interactive
